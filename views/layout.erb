<!DOCTYPE html>
<html>
<head>
	<title>Flight Track App</title>
	<link rel="stylesheet" type="text/css" href="../public/resources/css/reset.css">
	<link rel="stylesheet" type="text/css" href="../public/resources/css/style.css">
</head>
<body>
  <header>
    <h1>Track Your Flight</h1>
    <div class="message">
      <% if session[:success] %>
        <p class="success"><%= session.delete(:success) %></p>
      <% elsif session[:alert] %>
        <p class="alert"><%= session.delete(:alert) %></p>
      <% end %>
    </div>
    <nav>
    	<% unless session[:signed] %>
    	  <form action="/FlightTrackApp/users/sign" method="get">
    	  	<button type="submit" value="Sign in">Sign in</button>
    	  	<button type="submit" value="Register">Register</button>
    	  </form>
    	<% else %>
    	  	<form action="/FlightTrackApp" method="get">
    	  		<button type="submit" value="Sign out"></button>
    	  	</form>
    	<% end %>
    </nav>
  </header>
  <%== yield %>
  <footer></footer>
</body>
</html>